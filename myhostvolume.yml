apiVersion: v1
kind: Pod 
metadata:
   name: hostvolume
spec: 
  containers:
    - name: testcontainer
      image: centos
      command: ["/bin/bash" , "-c" , "sleep 15000"]
      volumeMounts:
          - mountPath: /tmp/hostpath
            name: testvolume
  volumes:
    - name: testvolume
      hostPath:
      path: /tmp/data           
